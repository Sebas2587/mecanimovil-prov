import { COLORS } from '@/app/design-system/tokens';

const g = COLORS.neutral.gray;

/** Base y “highlight” del esqueleto (tokens institucionales). */
export const SKELETON_BASE = g[100];
export const SKELETON_MUTED = g[200];
export const SKELETON_STRIP = g[50];
