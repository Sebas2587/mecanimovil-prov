# Deploy en Vercel vía Deploy Hook (no requiere VERCEL_TOKEN en GitHub).
name: Deploy to Vercel (prov)
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: |
          curl -fsS -X POST \
            "https://api.vercel.com/v1/integrations/deploy/prj_p5ArLTR43TKeYVxNtrNBd3RDEXRW/ncckJ5aige" \
            -H "Content-Type: application/json"
